# Encore Backend Environment Variables
# 
# Note: Encore uses its built-in secrets manager for sensitive values.
# Use `encore secret set` to configure secrets like API keys and passwords.
# See: https://encore.dev/docs/go/primitives/secrets
#
# For local development, you can override secrets using .secrets.local.cue
# See: https://encore.dev/docs/go/primitives/secrets#local-development

# External Database Connection (if using external PostgreSQL)
# Encore manages databases automatically, but you can connect to external databases
# POSTGRES_CONNECTION_STRING=postgresql://user:password@hostname:port/dbname?sslmode=require

# Artifacts MMO API Configuration
# ARTIFACTS_API_BASE_URL=https://api.artifacts-mmo.com
# ARTIFACTS_API_KEY=your-api-key-here

# Game State Cache Configuration
# REDIS_URL=redis://localhost:6379
# CACHE_TTL=3600

# Development Settings
# ENCORE_ENV=local
# LOG_LEVEL=debug
